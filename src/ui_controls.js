// ============================================================
// ui_controls.js — UI全般の初期化
// ============================================================

export function initUIControls(_map) {
  // ファイル読み込みボタンのイベントは data_import.js の initDataImport() で登録するため、
  // ここでは何もしない（二重登録を防ぐ）
}
